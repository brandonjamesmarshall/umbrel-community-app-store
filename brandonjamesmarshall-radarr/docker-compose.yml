version: "3.7"

# Media library: Docker-managed NFS volume backed by the NAS share,
# mounted at /media. Radarr's DB references /media/Movies (works as-is)
# and /Download/<category> (legacy paths from the NAS install — handled
# via Settings -> Download Clients -> Remote Path Mappings in the UI,
# where you map /Download -> /media/Download so imports of new downloads
# translate cleanly).

services:

  app_proxy:
    environment:
      # Full container name (see Sonarr compose for why we can't use bare "server").
      APP_HOST: brandonjamesmarshall-radarr_server_1
      APP_PORT: 7878
      # Whitelist API + push paths so mobile apps and Prowlarr can authenticate
      # via Radarr's API key directly, bypassing umbrelOS's session login.
      PROXY_AUTH_WHITELIST: /api,/signalr,/feed,/Content,/MediaCover,/initialize.json,/ping

  server:
    image: linuxserver/radarr:6.3.0@sha256:119aaa4a4f7349bcd2a136c5373a0d7925b5479915c7dfe0c0ad352db2a6d438
    restart: on-failure
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: "Etc/UTC"
    # Direct port for mobile/API clients — bypasses umbrelOS auth gateway.
    # Keep Radarr's Settings -> General -> Authentication enabled.
    ports:
      - "17878:7878"
    volumes:
      - ${APP_DATA_DIR}/data/config:/config
      - media:/media

volumes:
  media:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=192.168.50.111,nfsvers=4.1,rw,hard,noatime"
      device: ":/volume3/Plex Media"
